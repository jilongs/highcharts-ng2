import { PinEventCountChartDirective } from './pin-event-count-chart.directive';

describe('PinEventCountChartDirective', () => {
  it('should create an instance', () => {
    const directive = new PinEventCountChartDirective();
    expect(directive).toBeTruthy();
  });
});
